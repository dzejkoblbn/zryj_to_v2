<p id="notice"><%= notice %></p>

<h1>Lista produktów</h1>

<table>
  <thead>
    <tr>
      <th>Nazwa</th>
      <th>Opis</th>
      <th>Cena</th>
      <th>Czas przygotowania</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @produkts.each do |produkt| %>
      <tr>
        <td><%= produkt.nazwa_produktu %></td>
        <td><%= produkt.opis_produktu %></td>
        <td><%= produkt.cena %></td>
        <td><%= produkt.czas_przygotowania %></td>
        <td><%= link_to 'Show', produkt %></td>
        <% if logged_in? %>
        <td><%= link_to 'Edit', edit_produkt_path(produkt) %></td>
        <td><%= link_to 'Destroy', produkt, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %><% end %>
  </tbody>
</table>

<br>
<% if logged_in? %>
<%= link_to 'New Produkt', new_produkt_path %>
<% end %>
